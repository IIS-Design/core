name: CI

on:
  push:

jobs:
  deploy:
    permissions:
      contents: read
      packages: write

    uses: IIS-Design/.github/.github/workflows/container-deploy.yml@main

    with:
      app: core
      container-port: 3000
      smoke-path: /core/docs/content/carousel
